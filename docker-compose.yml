version: '3.8'

services:
  discord-bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./src:/app/src:ro
    environment:
      - NODE_ENV=production
